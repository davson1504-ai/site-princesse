import{promises as fs}from"node:fs";import path from"node:path";const file=path.join(process.cwd(),"data","availability.json");export type LocalDate={id:string;date:string;isPublished:boolean;startMinutes:number;endMinutes:number;breakStartMinutes:number|null;breakEndMinutes:number|null;slotIntervalMinutes:number;note:string|null;createdAt:string;updatedAt:string};let lock:Promise<void>=Promise.resolve();async function exclusive<T>(fn:()=>Promise<T>){const before=lock;let release!:()=>void;lock=new Promise(r=>release=r);await before;try{return await fn()}finally{release()}}export async function readLocalDates():Promise<LocalDate[]>{try{const text=await fs.readFile(file,"utf8");return text?JSON.parse(text):[]}catch(e){if((e as NodeJS.ErrnoException).code==="ENOENT")return[];throw e}}async function write(rows:LocalDate[]){await fs.mkdir(path.dirname(file),{recursive:true});const temp=`${file}.${crypto.randomUUID()}.tmp`;await fs.writeFile(temp,JSON.stringify(rows,null,2),"utf8");await fs.rename(temp,file)}export async function publishLocalDate(date:string){return exclusive(async()=>{const rows=await readLocalDates(),active=rows.filter(x=>x.isPublished&&x.date>=new Date().toISOString().slice(0,10));if(active.length>=7)throw new Error("Maximum de 7 dates publiées atteint");const now=new Date().toISOString();let row=rows.find(x=>x.date===date);if(row)Object.assign(row,{isPublished:true,updatedAt:now});else{row={id:crypto.randomUUID(),date,isPublished:true,startMinutes:540,endMinutes:1080,breakStartMinutes:null,breakEndMinutes:null,slotIntervalMinutes:30,note:null,createdAt:now,updatedAt:now};rows.push(row)}await write(rows);return row})}export async function patchLocalDate(id:string,data:Partial<LocalDate>){return exclusive(async()=>{const rows=await readLocalDates(),index=rows.findIndex(x=>x.id===id);if(index<0)throw new Error("Date introuvable");if(data.isPublished&&!rows[index].isPublished&&rows.filter(x=>x.isPublished).length>=7)throw new Error("Maximum de 7 dates publiées atteint");rows[index]={...rows[index],...data,updatedAt:new Date().toISOString()};await write(rows);return rows[index]})}
