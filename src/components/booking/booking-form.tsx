"use client";
import { useState } from "react";
import { useSearchParams } from "next/navigation";
import { services,hairstyles } from "@/data/site";
export function BookingForm(){const params=useSearchParams();const [busy,setBusy]=useState(false);const [error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const raw=Object.fromEntries(new FormData(e.currentTarget));const body={...raw,privacy:raw.privacy==="on"};const res=await fetch("/api/appointments",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const data=await res.json();if(res.ok)location.href=`/confirmation/${data.reference}?wa=${encodeURIComponent(data.whatsappUrl||"")}`;else{setError(data.error||"Impossible d’envoyer la demande");setBusy(false)}}return <form onSubmit={submit} className="grid gap-5 md:grid-cols-2">{error&&<p role="alert" className="md:col-span-2 rounded-xl bg-red-50 p-4 text-red-800">{error}</p>}<Field label="Nom complet" name="customerName"/><Field label="Téléphone" name="phone" type="tel"/><Field label="WhatsApp (format international)" name="whatsapp" type="tel"/><Field label="Email (facultatif)" name="email" type="email"/><Select label="Service" name="serviceId" defaultValue={params.get("service")||""} options={services.map(x=>[x.id,x.name])}/><Select label="Coiffure" name="hairstyleId" defaultValue={params.get("coiffure")||""} options={hairstyles.map(x=>[x.id,x.name])}/><Field label="Date" name="appointmentDate" type="date"/><Field label="Heure" name="appointmentTime" type="time"/><Field label="Localisation / adresse" name="location"/><Select label="Lieu" name="appointmentType" options={[["salon","Au salon"],["domicile","À domicile"]]}/><Select label="Contact préféré" name="preferredContactMethod" options={[["whatsapp","WhatsApp"],["telephone","Téléphone"],["email","Email"]]}/><label className="md:col-span-2">Message<textarea name="message" className="mt-2 min-h-28 w-full rounded-xl border border-black/15 bg-white p-3"/></label><input tabIndex={-1} autoComplete="off" name="website" className="absolute -left-[9999px]" aria-hidden="true"/><label className="md:col-span-2 flex gap-3 text-sm"><input required type="checkbox" name="privacy"/>J’accepte la politique de confidentialité.</label><button disabled={busy} className="md:col-span-2 rounded-full bg-[#231b1b] px-6 py-4 text-white disabled:opacity-50">{busy?"Envoi…":"Envoyer ma demande"}</button></form>}
function Field({label,name,type="text"}:{label:string;name:string;type?:string}){return <label>{label}<input required={name!=="email"} name={name} type={type} className="mt-2 w-full rounded-xl border border-black/15 bg-white p-3"/></label>}
function Select({label,name,options,defaultValue}:{label:string;name:string;options:string[][];defaultValue?:string}){return <label>{label}<select required name={name} defaultValue={defaultValue||options[0]?.[0]||""} className="mt-2 w-full rounded-xl border border-black/15 bg-white p-3">{options.map(([v,l])=><option value={v} key={v}>{l}</option>)}</select></label>}
