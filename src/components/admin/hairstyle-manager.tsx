"use client";import Image from"next/image";import{useState}from"react";type Hairstyle={id:string;name:string;mainImage:string;isActive:boolean;isFeatured:boolean};export function HairstyleManager({initial}:{initial:Hairstyle[]}){const[items,setItems]=useState(initial);async function save(item:Hairstyle,change:Partial<Hairstyle>){const r=await fetch(`/api/admin/hairstyles/${item.id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(change)});if(r.ok){const data=await r.json();setItems(xs=>xs.map(x=>x.id===item.id?data:x))}}return <div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{items.map(h=><article key={h.id} className="overflow-hidden rounded-2xl bg-white"><div className="relative aspect-[4/5]"><Image src={h.mainImage} alt={h.name} fill sizes="33vw" className="object-cover"/></div><div className="p-4"><h2 className="font-serif text-xl">{h.name}</h2><label className="mr-4"><input type="checkbox" checked={h.isActive} onChange={e=>save(h,{isActive:e.target.checked})}/> Actif</label><label><input type="checkbox" checked={h.isFeatured} onChange={e=>save(h,{isFeatured:e.target.checked})}/> Vedette</label></div></article>)}</div>}
