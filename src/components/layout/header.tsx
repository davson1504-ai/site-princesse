"use client";
import Link from "next/link";import {useState} from "react";import {Menu,X} from "lucide-react";
const links=[["/coiffures","Coiffures"],["/services","Services"],["/a-propos","À propos"],["/contact","Contact"]];
export function Header(){const[open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-black/5 bg-[#fffaf6]/90 backdrop-blur"><div className="mx-auto flex max-w-6xl items-center justify-between px-5 py-4"><Link href="/" className="font-serif text-xl tracking-wide" onClick={()=>setOpen(false)}>Princesse <span className="text-[#a86f43]">Coiffure</span></Link><nav className="hidden gap-6 md:flex" aria-label="Navigation principale">{links.map(([href,label])=><Link key={href} href={href} className="text-sm hover:text-[#a86f43]">{label}</Link>)}</nav><div className="flex items-center gap-2"><Link href="/rendez-vous" className="rounded-full bg-[#231b1b] px-4 py-2 text-sm text-white">Rendez-vous</Link><button className="grid size-10 place-items-center md:hidden" aria-label={open?"Fermer le menu":"Ouvrir le menu"} aria-expanded={open} onClick={()=>setOpen(x=>!x)}>{open?<X/>:<Menu/>}</button></div></div>{open&&<nav className="border-t bg-[#fffaf6] px-5 py-4 md:hidden" aria-label="Navigation mobile">{links.map(([href,label])=><Link onClick={()=>setOpen(false)} key={href} href={href} className="block border-b border-black/5 py-3">{label}</Link>)}</nav>}</header>}
