import Image from "next/image";import type{Metadata}from"next";import{products,site}from"@/data/site";import{productPrice}from"@/lib/pricing";
export const metadata:Metadata={title:"Produits capillaires",description:"Catalogue disponible sur demande, sans paiement en ligne."};
export default function Page(){return <div className="mx-auto max-w-6xl px-5 py-16"><p className="text-xs uppercase tracking-[.25em] text-[#9a624d]">Catalogue sans paiement en ligne</p><h1 className="mt-2 font-serif text-5xl">Produits capillaires</h1><p className="mt-4 text-black/65">Demandez un produit sur WhatsApp. La disponibilité est confirmée manuellement.</p><div className="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{products.map((p,i)=>{const price=productPrice(p.priceCents,p.priceStatus);const message=`Bonjour ${site.name}, je souhaite demander ${p.name}, affiché à ${price}. Pouvez-vous confirmer sa disponibilité ?`;return <article key={p.slug} className="overflow-hidden rounded-3xl bg-white shadow-sm"><div className="relative aspect-square bg-[#f4ece8]"><Image src={p.imagePath} alt={`${p.name} de la marque ${p.brand}`} fill priority={i===0} sizes="(max-width:640px) 100vw,(max-width:1024px) 50vw,33vw" className="object-contain p-3"/></div><div className="p-6"><p className="text-xs uppercase tracking-widest">{p.brand} · {p.category}</p><h2 className="mt-2 font-serif text-2xl">{p.name}</h2><p className="mt-2 text-sm text-black/65">{p.description}</p>{p.format&&<p className="mt-2 text-sm">{p.format}</p>}<p className="mt-4 font-semibold">{price}</p><a target="_blank" rel="noreferrer" href={`https://wa.me/33745238006?text=${encodeURIComponent(message)}`} className="mt-5 inline-block rounded-full bg-[#231b1b] px-5 py-3 text-sm text-white">Demander ce produit sur WhatsApp</a></div></article>})}</div><aside className="mt-10 rounded-2xl bg-amber-50 p-5 text-sm leading-6">Les informations produit sont fournies à titre descriptif. Vérifiez la composition et les précautions indiquées par le fabricant. En cas de problème de cuir chevelu ou de chute persistante, demandez conseil à un professionnel de santé.</aside></div>}
