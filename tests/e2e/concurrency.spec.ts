import{test,expect}from"@playwright/test";
import {findAvailableSlot} from "./availability-helper";
test("deux demandes simultanées ne réservent pas le même créneau",async({request},testInfo)=>{test.skip(testInfo.project.name!=="chromium","Un seul projet exécute ce test de concurrence");const slot=await findAvailableSlot(request,"tresses",240);const base={phone:"+33700000001",whatsapp:"+33700000001",email:"",serviceId:"tresses",hairstyleId:"couronne",appointmentDate:slot.date,appointmentTime:slot.time,location:"Paris",appointmentType:"salon",preferredContactMethod:"whatsapp",message:"Test automatisé",website:"",privacy:true};const responses=await Promise.all([request.post("/api/appointments",{data:{...base,customerName:"Concurrence A"}}),request.post("/api/appointments",{data:{...base,customerName:"Concurrence B"}})]);expect(responses.map(response=>response.status()).sort()).toEqual([201,409])});
