# Futur domaine

Ajouter le domaine dans **Vercel > Project > Settings > Domains**, configurer les enregistrements DNS indiqués par Vercel, puis mettre à jour `NEXT_PUBLIC_SITE_URL`. Conserver l’URL actuelle jusqu’à validation DNS et ne modifier aucun secret.
